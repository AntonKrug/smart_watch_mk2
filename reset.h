#ifndef SMARTWATCH_RESET_H
#define SMARTWATCH_RESET_H

// Set all the internal peripherals of the ATmega88PA with an 8MHz clock into a good known state
extern void systemPeripheralsSetup(void);

#endif
